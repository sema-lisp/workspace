# Copy to .env (gitignored). Loaded by the workspace Jakefile via @dotenv, so the
# mono's LLM/provider recipes (jake sema.test-providers, jake sema.llm-stress, …)
# pick up keys without polluting your shell.
ANTHROPIC_API_KEY=
OPENAI_API_KEY=
GEMINI_API_KEY=
MISTRAL_API_KEY=
